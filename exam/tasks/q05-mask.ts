/**
 * 第5問（五段）Jev に送る前に、個人情報をコードで隠せ。
 *
 * 条件:
 * - 電話番号（例: 090-1234-5678、03-1234-5678、09012345678）を ［電話番号］ に置き換える
 * - メールアドレス（例: taro@example.com）を ［メール］ に置き換える
 * - それ以外の文字は変えない
 *
 * ----
 * Q5 (5th Dan) Mask personal data in code before sending it to Jev.
 *
 * Requirements:
 * - replace phone numbers (e.g. 090-1234-5678, 03-1234-5678, 09012345678) with ［電話番号］
 * - replace email addresses (e.g. taro@example.com) with ［メール］
 * - leave every other character unchanged
 * (the test checks these exact replacement strings)
 */
export function maskPersonalInfo(_text: string): string {
  throw new Error("未実装 / not implemented: 第5問 / Q5");
}
