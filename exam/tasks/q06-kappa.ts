/**
 * 第6問（六段）2人のラベルの一致度 Cohen の κ を計算せよ。
 *
 * κ = (観測された一致率 − 偶然の一致率) ÷ (1 − 偶然の一致率)
 * 偶然の一致率 = Σ_カテゴリ (Aがそのカテゴリを付けた割合 × Bがそのカテゴリを付けた割合)
 * 偶然の一致率が 1 のときは 1 を返す。長さが違えば例外を投げる。
 */
export function kappa<T>(_a: T[], _b: T[]): number {
  throw new Error("未実装: 第6問");
}
